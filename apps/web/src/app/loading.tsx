export default function Loading() {
  return (
    <div className="container section" aria-live="polite">
      <div className="glass-panel" style={{ minHeight: 280, borderRadius: 16, padding: 24 }}>Đang tải nội dung...</div>
    </div>
  );
}
